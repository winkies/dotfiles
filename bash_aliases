# ~/.bash_aliases

# some more ls aliases
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'

# emacs alias
alias ne="emacs -nw"

# git alias
alias gl="git log"
alias gs="git status"
alias gb="git branch -vva"
alias gc="git clone $@"